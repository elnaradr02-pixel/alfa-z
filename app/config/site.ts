/**
 * Единый источник правды по изменяемым данным сайта.
 * Меняете здесь — обновляется во всех местах (баннер, расписание, курсы, FAQ).
 */
export const siteConfig = {
  cohort: {
    /** ISO-дата старта потока — для машинной логики/разметки. */
    startISO: "2026-07-01",
    /** Полная подпись даты: «1 июля 2026». Меняйте это поле для нового потока. */
    dateLabel: "1 июля 2026",
    /** Число + месяц отдельно: «1 июля» (для крупной даты в карточке). */
    dayMonth: "1 июля",
    /** Год отдельной строкой: «2026 года». */
    year: "2026 года",
    /** Время начала занятий. */
    time: "20:00",
  },

  /**
   * Флаг чернового контента. true — показываем пометки-заглушки
   * («отзывы — пример» и т.п.) на стейджинге. В проде держать false,
   * чтобы дев-заметки не утекали к пользователю.
   */
  draftContent: false,
} as const;
